from section_factory import make_content_router

MENU_TITLE = "LLM Arena & Comparação de Modelos"
MENU_ICON = "fa-solid fa-trophy"

SECTION = {
    "title": MENU_TITLE,
    "description": "Comparar modelos lado a lado e escolher pelo trade-off custo × qualidade × latência.",
    "eyebrow": "Construir",
    "eyebrow_icon": "fa-solid fa-screwdriver-wrench",
}

BODY = {
    "pillars": ["Modelagem", "Performance"],
    "requisitos": [
        {"id": "1.1.5", "titulo": "Comparação A/B/C de modelos no mesmo prompt"},
        {"id": "1.4", "titulo": "Catálogo de modelos + fine-tuning"},
    ],
    "resumo": (
        "A Contoso precisa decidir entre dezenas de modelos sem refazer o agente. O <strong>Foundry Models</strong> "
        "expõe um catálogo unificado (OpenAI, Microsoft, Meta, Mistral, etc.) e o <strong>LLM Arena / Model "
        "Leaderboard</strong> permite rodar o mesmo prompt em vários modelos com métricas comparáveis."
    ),
    "bullets": [
        "Catálogo único: GPT-4.x, GPT-5, o-series, Phi, Llama, Mistral, modelos especializados.",
        "Comparação <em>side-by-side</em> com os mesmos inputs e evaluators.",
        "Trade-off explícito: custo / latência / qualidade / risco regulatório.",
        "Fine-tune, distilation e RLHF/DPO no mesmo Foundry para casos onde o agente precisa de modelo dedicado.",
    ],
    "passos_titulo": "Demonstrar no Foundry",
    "passos": [
        {
            "titulo": "Abrir Foundry → Models → Leaderboard",
            "html": "Mostrar comparativo de qualidade vs custo dos modelos elegíveis para o agente Contoso.",
        },
        {
            "titulo": "Rodar o prompt do Curador em 3 modelos simultaneamente",
            "html": "Mesma entrada, 3 colunas; mostrar scores de evaluator por modelo.",
        },
        {
            "titulo": "Trocar o modelo do agente sem alterar prompt",
            "html": "Editar <code>agent.yaml</code> alterando o campo <code>model</code> — deploy reaproveita evaluators.",
        },
    ],
    "mensagem_chave": (
        "<strong>Decisão por dado, não por hype.</strong> A escolha de modelo passa a ser um trade-off "
        "explícito e auditável — e mudar de modelo é uma linha de YAML."
    ),
    "extra_html": (
        '<figure class="section-figure" style="margin-top: 22px;">'
        '<img src="/static/images/compare-model-overview.png" '
        'alt="Foundry Model Leaderboard / Compare Models — comparação lado a lado de qualidade, custo e latência">'
        '<figcaption>Foundry &middot; <em>Compare Models</em> &mdash; mesmo prompt rodando em vários modelos, '
        'com qualidade, custo e latência lado a lado. É a tela que materializa a decisão por dado.</figcaption>'
        '</figure>'
    ),
}

BODY_I18N = {
    "en": {
        "body_localized": True,
        "resumo": (
            "Contoso needs to choose between dozens of models without rebuilding the agent. <strong>Foundry Models</strong> "
            "exposes a unified catalog (OpenAI, Microsoft, Meta, Mistral, etc.) and the <strong>LLM Arena / Model "
            "Leaderboard</strong> lets you run the same prompt across many models with comparable metrics."
        ),
        "bullets": [
            "Single catalog: GPT-4.x, GPT-5, o-series, Phi, Llama, Mistral, specialized models.",
            "<em>Side-by-side</em> comparison with the same inputs and evaluators.",
            "Explicit trade-off: cost / latency / quality / regulatory risk.",
            "Fine-tune, distillation and RLHF/DPO in the same Foundry for agents that need a dedicated model.",
        ],
        "passos_titulo": "Live demo in Foundry",
        "passos": [
            {
                "titulo": "Open Foundry → Models → Leaderboard",
                "html": "Show quality vs cost comparison of eligible models for the Contoso agent.",
            },
            {
                "titulo": "Run the Curator prompt on 3 models simultaneously",
                "html": "Same input, 3 columns; show evaluator scores per model.",
            },
            {
                "titulo": "Switch the agent's model without changing the prompt",
                "html": "Edit <code>agent.yaml</code> changing the <code>model</code> field — deploy reuses evaluators.",
            },
        ],
        "mensagem_chave": (
            "<strong>Decisions based on data, not hype.</strong> Model choice becomes an explicit, auditable "
            "trade-off — and switching models is one YAML line."
        ),
        "extra_html": (
            '<figure class="section-figure" style="margin-top: 22px;">'
            '<img src="/static/images/compare-model-overview.png" '
            'alt="Foundry Model Leaderboard / Compare Models — side-by-side comparison of quality, cost and latency">'
            '<figcaption>Foundry &middot; <em>Compare Models</em> &mdash; the same prompt running across several models, '
            'with quality, cost and latency side by side. This is the screen that materializes data-driven decisions.</figcaption>'
            '</figure>'
        ),
    },
    "es": {
        "body_localized": True,
        "resumo": (
            "Contoso necesita decidir entre decenas de modelos sin rehacer el agente. <strong>Foundry Models</strong> "
            "expone un catálogo unificado (OpenAI, Microsoft, Meta, Mistral, etc.) y el <strong>LLM Arena / Model "
            "Leaderboard</strong> permite ejecutar el mismo prompt en varios modelos con métricas comparables."
        ),
        "bullets": [
            "Catálogo único: GPT-4.x, GPT-5, o-series, Phi, Llama, Mistral, modelos especializados.",
            "Comparación <em>side-by-side</em> con los mismos inputs y evaluadores.",
            "Trade-off explícito: costo / latencia / calidad / riesgo regulatorio.",
            "Fine-tune, destilación y RLHF/DPO en el mismo Foundry para casos donde el agente necesita un modelo dedicado.",
        ],
        "passos_titulo": "Demostrar en Foundry",
        "passos": [
            {
                "titulo": "Abrir Foundry → Models → Leaderboard",
                "html": "Mostrar comparativa de calidad vs costo de los modelos elegibles para el agente Contoso.",
            },
            {
                "titulo": "Ejecutar el prompt del Curador en 3 modelos simultáneamente",
                "html": "Misma entrada, 3 columnas; mostrar scores de evaluadores por modelo.",
            },
            {
                "titulo": "Cambiar el modelo del agente sin alterar el prompt",
                "html": "Editar <code>agent.yaml</code> cambiando el campo <code>model</code> — el deploy reutiliza los evaluadores.",
            },
        ],
        "mensagem_chave": (
            "<strong>Decisión por dato, no por hype.</strong> La elección del modelo se convierte en un trade-off "
            "explícito y auditable — y cambiar de modelo es una línea de YAML."
        ),
        "extra_html": (
            '<figure class="section-figure" style="margin-top: 22px;">'
            '<img src="/static/images/compare-model-overview.png" '
            'alt="Foundry Model Leaderboard / Compare Models — comparación lado a lado de calidad, costo y latencia">'
            '<figcaption>Foundry &middot; <em>Compare Models</em> &mdash; el mismo prompt ejecutándose en varios modelos, '
            'con calidad, costo y latencia lado a lado. Es la pantalla que materializa la decisión por dato.</figcaption>'
            '</figure>'
        ),
    },
}

router = make_content_router("ato1_llm_arena", SECTION, BODY, body_i18n=BODY_I18N)
__all__ = ["router", "MENU_TITLE", "MENU_ICON"]
